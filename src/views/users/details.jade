.content.users-details
    a(ui-sref="app.users.list") &lt; Back
    
    div.text-center
        img(ng-src="{{user.avatar}}")
        
        h2 {{user.name}}
        
        p {{user.bio}}
        
        button(ui-sref="app.users.edit") Edit
